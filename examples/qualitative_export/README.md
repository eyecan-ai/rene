# Export qualitatives in usefulf format

Launch:

```bash
python qualitative_export.py -i ${QUALI_ROOT_FOLDER} -o /tmp/qualitatives/  -r 480
```

Change in code the Indices / Cfg part to export different indices.