# Create video with easy/hard splits effect


Launch:

```bash
python create_video_splits.py -i ${RENE_ROOT_FOLDER} -c cfgs/video_with_splits.yml  -o /tmp/output_frames --debug
```

remove `--debug` to write faster!

